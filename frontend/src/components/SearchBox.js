import React, { useState } from 'react'
import { Form, Button } from 'react-bootstrap'

const SearchBox = () => {
    return (
        <div>
            
        </div>
    )
}

export default SearchBox
